#include <gtest/gtest.h>
#include <chart.h>

using namespace chart;

TEST(Test1, Test) {
	Canvas chart;
	EXPECT_EQ(1,1);
}

TEST(Test1, Test2) {
	Canvas chart;
	EXPECT_EQ(1,1);
}

TEST(Test1, Test3) {
	Canvas chart;
	EXPECT_EQ(1,1);
}

TEST(Test2, A) {
	Canvas chart;
	EXPECT_EQ(1,1);
}

TEST(Test2, B) {
	Canvas chart;
	EXPECT_EQ(1,1);
}


